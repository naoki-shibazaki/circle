<% set_meta_tags title: "質問コーナー",

				og: { 
					title: "登録不要！匿名で質問ができます！",
					description: "", 
					image: image_url('/images/qa_top.jpg')
				},

				twitter: { card: 'summary_large_image'} 
%>



<div class="main users-index">
  <div class="container bg_white">
  	   <%= render "/layouts/breadcrumb" %>


		<h1 class="e_header_title">質問コーナー</h1>


	<div class="search_qa">	 

			<%= search_form_for @search do |f| %>
			  			
			    <%= f.search_field :question_cont, placeholder: "質問を検索する（1単語のみ）", class: "event_search" %>

				<div class="search_icon">
				  	<%= button_tag type: 'submit' do %>
				  		<i class="fa fa-search "></i>
				  	<% end %>
			  	</div>
			
			<% end %>

	</div>



	<% if @search_questions.any? %>
		<div class="qa_card_warp">

			<ul>
				<% @search_questions.each do |search_question| %>
				
						<li class="ribbon-wrapper">

							<% @events.each do |event| %>
								<% if search_question.event_id == event.id %>

									<%= link_to "/#{event.ruby}/qa/#{search_question.id}" do %>

                    <div class="qa_large">
                      <div class="user_slide_qa">
                        <div class="user_slide_qa_wrap">
                          <div class="user_slide_qa_content_wrap">
                            <h2><%= search_question.question %></h2>
                          </div>
                        </div>
                      </div>
                    </div>

									<% end %>

								<% end %>
							<% end %>

						</li>

				<% end %>
			</ul>

		</div>
	<% end %>

	<% if @search_questions.nil? %>

		<div class="qa_card_warp">
			<ul>
				<% @event_questions.each do |event_question| %>
				
						<li class="ribbon-wrapper">

							<% @events.each do |event| %>
								<% if event_question.event_id == event.id %>

									<%= link_to "/#{event.ruby}/qa/#{event_question.id}" do %>
                    <div class="qa_large">
                      <div class="user_slide_qa">
                        <div class="user_slide_qa_wrap">
                          <div class="user_slide_qa_content_wrap">
                            <h2><%= event_question.question %></h2>
                          </div>
                        </div>
                      </div>
                    </div>
									<% end %>

								<% end %>
							<% end %>

						</li>

				<% end %>
			</ul>
		</div>

	<% end %>
	

  </div>
</div>
