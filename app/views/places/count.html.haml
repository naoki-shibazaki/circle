- set_meta_tags noindex: true, nofollow: true
.main.users-index
  .container
    = render "/layouts/breadcrumb"
    %h1.users-heading 投稿数
    .users-index-item
      - @places.each do |place|
        ["ID",投稿数]： #{place}
        %br/
