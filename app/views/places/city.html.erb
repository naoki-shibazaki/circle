<% set_meta_tags title: "#{@prefecture.name}#{@city.name}にある#{@event.name}のコート情報一覧" %>
<div class="main users-index">
  <div class="container">

    <%= render "/layouts/breadcrumb" %>
    <div class="e_header_wrap">
 	  <img src="/images/places/e_p_<%= @event.ruby %>.jpg" alt="<%= @event.name %>" >
      <div class="link_txt">
        <h1 class="e_header_title">
		    <%= @prefecture.name %><%= @city.name %>にある<br><%= @event.name %>のコート情報一覧
        </h1>
      </div>
    </div>

    <%= render "/places/places_list" %>


  </div>
</div>