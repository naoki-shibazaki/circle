<div class="main users-index">
  <div class="container">

	  	<div class="users-index-item">

	  		<%= render "/schedules/profile" %>


				<div class="contact_wrap">

					<% if params[:count] == "line" %>

						<a href="<%= @user.line_id %>">
							<div class="contact-button line">LINEで連絡する</div>
						</a>
						

				  	<% elsif params[:count] == "mail" %>
						
						<a href="mailto:<%= @data.email %>?subject=<%= @mail_title %>&amp;body=<%= @mail_message %>">
							<div class="contact-button mail">メールで連絡する</div>
						</a>
						
				  	<% else %>


				  	<% end %>

				 </div>

			


			

	
	  	</div>

  </div>
</div>