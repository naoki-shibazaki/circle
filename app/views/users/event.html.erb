<% set_meta_tags title: "【#{@time.strftime("%Y")}年版】#{@event.name}のサークル一覧" %>

<div class="main users-index">
  <div class="container">
 	<%= render "/layouts/breadcrumb" %>
    <h1 class="users-heading"><%= @event.name %>のサークル一覧</h1>

	<% if @hit != 0 %>
		<div class="search_area">
		    <%= render "prefectures_list.html.erb" %>
		</div>
	<% end %>

	<% if @hit == 0 %>
		<%= render "/users/no_hit.html.erb" %>
		
		<h2 class="users-heading">全国のサークル一覧</h2>
	<% end %>

    <%= render "users_list.html.erb" %>

  </div>
</div>