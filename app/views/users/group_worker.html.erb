<% set_meta_tags title: "#{@prefecture.name}で活動している#{@event.name}の#{@group.name}サークル一覧" %>

<div class="main users-index">
  <div class="container">
    <%= render "/layouts/breadcrumb" %>
    <h1 class="users-heading">
      <%= @prefecture.name %>で活動している<br>
    	<%= @event.name %>の<%= @group.name %>サークル一覧
    </h1>

	<% if @hit == 0 %>
		<%= render "/users/no_hit.html.erb" %>
		
		<h2 class="users-heading">全国のサークル一覧</h2>
	<% end %>
 
    <%= render "/users/users_list.html.erb" %>
  
  </div>
</div>